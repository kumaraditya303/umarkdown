name: Upload Python Package

on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    name: Deploy
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.7", "3.8", "3.9"]
        os: [windows-latest, macos-latest, macos-11.0]
        include:
          - os: ubuntu-latest
            python-version: 3.9

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install twine wheel
      - name: Build ${{ matrix.os }} wheel
        if: matrix.os != 'ubuntu-latest'
        run: python setup.py sdist bdist_wheel
      - name: Build Linux wheel
        if: matrix.os == 'ubuntu-latest'
        run: |
          mkdir -p dist
          docker buildx build --platform linux/arm64 --output tmpwheelhouse -f scripts/Dockerfile.aarch .
          mv tmpwheelhouse/wheelhouse/*.whl dist/
          docker run -v `pwd`:/io quay.io/pypa/manylinux2014_x86_64 /io/scripts/manylinux.sh
          docker run -v `pwd`:/io quay.io/pypa/manylinux2014_i686 /io/scripts/manylinux.sh
      - name: Upload Wheels to PyPI
        env:
          TWINE_USERNAME: ${{ secrets.PYPI_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
        run: twine upload --skip-existing dist/*
      - name: Upload wheels
        uses: actions/upload-artifact@v2
        with:
          name: wheels
          path: dist/*.whl
